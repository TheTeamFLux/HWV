services:
  - type: web
    name: sumquiz-api
    env: java
    buildCommand: ./gradlew clean bootJar -x test
    startCommand: java -Xms64m -Xmx192m -XX:MaxMetaspaceSize=96m -XX:+UseSerialGC -XX:+ExitOnOutOfMemoryError -jar build/libs/backend-0.0.1-SNAPSHOT.jar
    healthCheckPath: /health
