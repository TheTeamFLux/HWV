services:
  - type: web
    name: sumquiz-api
    env: java
    buildCommand: ./gradlew clean bootJar -x test
    startCommand: java -Xms64m -Xmx224m -XX:MaxMetaspaceSize=128m -XX:+UseSerialGC -XX:+ExitOnOutOfMemoryError -jar build/libs/backend-0.0.1-SNAPSHOT.jar
    healthCheckPath: /health
